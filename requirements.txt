# --- Çekirdek: veri ve modelleme ---
yfinance>=0.2.36
pandas>=2.1.0
numpy>=1.26.0
arch>=6.2.0
scipy>=1.11.0

# --- Görselleştirme ---
matplotlib>=3.8.0
plotly>=5.18.0

# --- Pipeline ---
pyyaml>=6.0.1
requests>=2.31.0
beautifulsoup4>=4.12.0

# --- Opsiyonel (henüz aktif değil, v2 için) ---
# shap>=0.44.0        # GARCH için doğrudan değil, proxy modeller için
# lime>=0.2.0         # Alternatif XAI
# langchain>=0.1.0    # LLM pipeline'ı Ollama yerine OpenAI/Claude'a taşımak için
